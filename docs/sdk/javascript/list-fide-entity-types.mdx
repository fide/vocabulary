---
title: "listFideEntityTypes"
description: "List all defined Fide entity type definitions."
full: true
---

import { SDKFunctionPageInteractive } from '@/components/sdk-layout/sdk-function-page-interactive';

<SDKFunctionPageInteractive data={{"name":"listFideEntityTypes","description":"List all defined Fide entity type definitions.","section":"Vocabulary","signatures":[{"description":"List all defined Fide entity type definitions.","parameters":[],"returnType":"FideEntityTypeSpec[]","returnDescription":"Entity type definitions in source order.","infoCallout":{"title":"Node Runtime Required","body":"This function requires a Node.js runtime and is disabled in the browser docs preview."},"referencedTypes":[{"name":"FideEntityTypeSpec","description":"","type":"FideEntityTypeSpec","entries":{"code":{"description":"","type":"\"00\" | \"01\" | \"02\" | \"10\" | \"11\" | \"12\" | \"20\" | \"21\" | \"22\" | \"30\" | \"31\" | \"40\" | \"41\" | \"42\" | \"43\" | \"a0\" | \"a1\" | \"a2\" | \"a3\" | \"b1\" | \"b2\" | \"b3\" | \"b4\" | \"b0\" | \"b5\" | \"c0\" | \"c1\"","required":true},"layer":{"description":"","type":"\"Graph Structure\" | \"Agents\" | \"Network Anchors\" | \"Knowledge\" | \"Spacetime\" | \"Literals\"","required":true},"standards":{"description":"","type":"readonly [\"rdf:Statement\"] | readonly [\"rdf:Property\"] | readonly [\"rdf:Property\", \"owl:SymmetricProperty\"] | readonly [\"schema:Person\"] | readonly [\"org:Organization\"] | readonly [\"schema:SoftwareApplication\", \"prov:SoftwareAgent\"] | readonly [\"schema:WebPage\", \"schema:WebSite\", \"schema:EntryPoint\"] | readonly [\"schema:OnlineAccount\"] | readonly [\"sec:PublicKey\", \"sec:publicKeyMultibase\"] | readonly [\"schema:CreativeWork\"] | readonly [\"skos:Concept\", \"schema:DefinedTerm\"] | readonly [\"schema:Place\", \"schema:VirtualLocation\"] | readonly [\"schema:Event\"] | readonly [\"schema:Action\", \"prov:Activity\"] | readonly [\"dcmitype:PhysicalObject\"] | readonly [\"rdf:Literal\", \"xsd:string\"] | readonly [\"rdf:Literal\", \"xsd:integer\"] | readonly [\"rdf:Literal\", \"xsd:decimal\"] | readonly [\"rdf:Literal\", \"xsd:boolean\"] | readonly [\"rdf:Literal\", \"xsd:date\"] | readonly [\"rdf:Literal\", \"xsd:gYearMonth\"] | readonly [\"rdf:Literal\", \"xsd:gYear\"] | readonly [\"rdf:Literal\", \"xsd:time\"] | readonly [\"rdf:Literal\", \"xsd:dateTime\"] | readonly [\"rdf:Literal\", \"xsd:duration\"] | readonly [\"rdf:Literal\", \"xsd:anyURI\"] | readonly [\"rdf:Literal\", \"rdf:JSON\"]","required":true},"standardFit":{"description":"","type":"\"Exact\" | \"Close\" | \"Broad\"","required":true},"description":{"description":"","type":"\"A record of a subject-property-object assertion.\" | \"A property where the ordered pair (subject, object) is semantically significant.\" | \"A property where subject-object order is semantically interchangeable. Reversing subject and object preserves meaning and MUST resolve to the same canonical Statement identity.\" | \"A person (alive, dead, undead, or fictional).\" | \"A durable collective of people, software agents, or both that can act as a unit.\" | \"A running process that acts as an independent actor: bot, AI agent, smart contract. Can hold keys and sign.\" | \"A resource identified primarily by addressability or resolution, such as a URL, URI, or CID.\" | \"An authority-based account principal (e.g., an email address or a specific GitHub, Google, or Mastodon user ID). The identity exists by the permission of a host system's database.\" | \"A math-based account principal (e.g., an EVM address, Bitcoin wallet, or did:key). The identity exists mathematically and is authenticated via mathematical signature keys.\" | \"The most generic kind of creative work, including books, movies, photographs, software programs, etc. Non-expressive digital artifacts (binaries, model weights, dataset snapshots) are CreativeWork with refinement via Statements (artifactKind, mediaType, generatedBy, derivedFrom).\" | \"A socially or formally recognized unit of meaning, such as a term, topic, category, label, initiative, or other abstract idea.\" | \"A spatial context that can contain presence, co-location, and interaction. Includes physical locations and persistent virtual environments (e.g., a concert venue, a Discord server or channel, a Zoom room, a VRChat instance). Physicality/virtuality is asserted via Statements, not the base type.\" | \"An occurrence or happening bounded in time. Can be observed/aggregated; may be caused by Actions, systems, or nature. Occurs at a Place (physical or virtual).\" | \"A discrete assertion or commitment attributable to an Agent. Often produces or records a change (e.g., a transaction, API call, signature). Can be linked to Events via Statements (causes, records, resultsIn).\" | \"A tangible, inanimate physical object (e.g., hardware, vehicle, physical document, natural object). Physical only. Use Statements (madeBy, manufactured=true) if man-made vs natural matters.\" | \"A plain text sequence of characters.\" | \"A whole number.\" | \"A decimal or floating-point number.\" | \"A true or false value.\" | \"A calendar date in canonical `YYYY-MM-DD` form.\" | \"A calendar year and month in canonical `YYYY-MM` form.\" | \"A calendar year in canonical `YYYY` form.\" | \"A time of day (formatted according to ISO 8601, represented in UTC).\" | \"A specific point in time in canonical UTC `YYYY-MM-DDTHH:MM:SSZ` form.\" | \"A calculated amount of elapsed time (ISO 8601 duration format).\" | \"A formalized Uniform Resource Identifier string.\" | \"A structured JSON object represented in canonical string form.\"","required":true},"litmus":{"description":"","type":"\"Not a verified fact; verification is application-level. Not what it represents.\" | \"Use when reversing subject and object changes the meaning and produces a different Statement identity.\" | \"Use when `A -> p -> B` means the same thing as `B -> p -> A`, such as identity or mutual-peer relations.\" | \"Not an account or organization.\" | \"Not a single person or software agent. Legal incorporation is not required, but a legal company is always an Organization. A branded initiative, product line, or program is usually a Concept unless it has its own durable collective agency. Not a query-defined group.\" | \"Not a person or organization. Not static passive code (use CreativeWork for repos, images, logic).\" | \"Not a subset or account. Not an inhabitable spatial context (use Place).\" | \"Authenticated by a host authority's database. This represents the account principal itself, not the agent or agents that operate it.\" | \"Authenticated via mathematical signature keys, independent of a host authority.\" | \"Not a raw value or formal concept. Not a running agent instance (use SoftwareAgent). Not a resource where location is primary identifier (use NetworkResource).\" | \"Not a plain raw value. Use this for labels, brands, programs, or initiatives that do not have their own durable collective agency.\" | \"Not a happening or a physical artifact. Can an Actor have presence there? Is its identity a spatial address rather than a document location or account?\" | \"Not a specific exertion of agency (Action).\" | \"Not a broad temporal happening (Event); must be driven by an Agent.\" | \"Not a location (where). Not a digital artifact (use CreativeWork).\" | \"Not a Concept.\" | \"Not a decimal or string.\" | \"Not an integer.\" | \"Not a number or string.\" | \"Not a datetime or time.\" | \"Not a full date or year alone.\" | \"Not a year-month or full date.\" | \"Not a date or datetime.\" | \"Not a date or time alone.\" | \"Not a datetime.\" | \"Not plain text.\" | \"Not a string.\"","required":true}}}]}],"constants":[],"referencedTypes":[{"name":"FideEntityTypeSpec","description":"","type":"FideEntityTypeSpec","entries":{"code":{"description":"","type":"\"00\" | \"01\" | \"02\" | \"10\" | \"11\" | \"12\" | \"20\" | \"21\" | \"22\" | \"30\" | \"31\" | \"40\" | \"41\" | \"42\" | \"43\" | \"a0\" | \"a1\" | \"a2\" | \"a3\" | \"b1\" | \"b2\" | \"b3\" | \"b4\" | \"b0\" | \"b5\" | \"c0\" | \"c1\"","required":true},"layer":{"description":"","type":"\"Graph Structure\" | \"Agents\" | \"Network Anchors\" | \"Knowledge\" | \"Spacetime\" | \"Literals\"","required":true},"standards":{"description":"","type":"readonly [\"rdf:Statement\"] | readonly [\"rdf:Property\"] | readonly [\"rdf:Property\", \"owl:SymmetricProperty\"] | readonly [\"schema:Person\"] | readonly [\"org:Organization\"] | readonly [\"schema:SoftwareApplication\", \"prov:SoftwareAgent\"] | readonly [\"schema:WebPage\", \"schema:WebSite\", \"schema:EntryPoint\"] | readonly [\"schema:OnlineAccount\"] | readonly [\"sec:PublicKey\", \"sec:publicKeyMultibase\"] | readonly [\"schema:CreativeWork\"] | readonly [\"skos:Concept\", \"schema:DefinedTerm\"] | readonly [\"schema:Place\", \"schema:VirtualLocation\"] | readonly [\"schema:Event\"] | readonly [\"schema:Action\", \"prov:Activity\"] | readonly [\"dcmitype:PhysicalObject\"] | readonly [\"rdf:Literal\", \"xsd:string\"] | readonly [\"rdf:Literal\", \"xsd:integer\"] | readonly [\"rdf:Literal\", \"xsd:decimal\"] | readonly [\"rdf:Literal\", \"xsd:boolean\"] | readonly [\"rdf:Literal\", \"xsd:date\"] | readonly [\"rdf:Literal\", \"xsd:gYearMonth\"] | readonly [\"rdf:Literal\", \"xsd:gYear\"] | readonly [\"rdf:Literal\", \"xsd:time\"] | readonly [\"rdf:Literal\", \"xsd:dateTime\"] | readonly [\"rdf:Literal\", \"xsd:duration\"] | readonly [\"rdf:Literal\", \"xsd:anyURI\"] | readonly [\"rdf:Literal\", \"rdf:JSON\"]","required":true},"standardFit":{"description":"","type":"\"Exact\" | \"Close\" | \"Broad\"","required":true},"description":{"description":"","type":"\"A record of a subject-property-object assertion.\" | \"A property where the ordered pair (subject, object) is semantically significant.\" | \"A property where subject-object order is semantically interchangeable. Reversing subject and object preserves meaning and MUST resolve to the same canonical Statement identity.\" | \"A person (alive, dead, undead, or fictional).\" | \"A durable collective of people, software agents, or both that can act as a unit.\" | \"A running process that acts as an independent actor: bot, AI agent, smart contract. Can hold keys and sign.\" | \"A resource identified primarily by addressability or resolution, such as a URL, URI, or CID.\" | \"An authority-based account principal (e.g., an email address or a specific GitHub, Google, or Mastodon user ID). The identity exists by the permission of a host system's database.\" | \"A math-based account principal (e.g., an EVM address, Bitcoin wallet, or did:key). The identity exists mathematically and is authenticated via mathematical signature keys.\" | \"The most generic kind of creative work, including books, movies, photographs, software programs, etc. Non-expressive digital artifacts (binaries, model weights, dataset snapshots) are CreativeWork with refinement via Statements (artifactKind, mediaType, generatedBy, derivedFrom).\" | \"A socially or formally recognized unit of meaning, such as a term, topic, category, label, initiative, or other abstract idea.\" | \"A spatial context that can contain presence, co-location, and interaction. Includes physical locations and persistent virtual environments (e.g., a concert venue, a Discord server or channel, a Zoom room, a VRChat instance). Physicality/virtuality is asserted via Statements, not the base type.\" | \"An occurrence or happening bounded in time. Can be observed/aggregated; may be caused by Actions, systems, or nature. Occurs at a Place (physical or virtual).\" | \"A discrete assertion or commitment attributable to an Agent. Often produces or records a change (e.g., a transaction, API call, signature). Can be linked to Events via Statements (causes, records, resultsIn).\" | \"A tangible, inanimate physical object (e.g., hardware, vehicle, physical document, natural object). Physical only. Use Statements (madeBy, manufactured=true) if man-made vs natural matters.\" | \"A plain text sequence of characters.\" | \"A whole number.\" | \"A decimal or floating-point number.\" | \"A true or false value.\" | \"A calendar date in canonical `YYYY-MM-DD` form.\" | \"A calendar year and month in canonical `YYYY-MM` form.\" | \"A calendar year in canonical `YYYY` form.\" | \"A time of day (formatted according to ISO 8601, represented in UTC).\" | \"A specific point in time in canonical UTC `YYYY-MM-DDTHH:MM:SSZ` form.\" | \"A calculated amount of elapsed time (ISO 8601 duration format).\" | \"A formalized Uniform Resource Identifier string.\" | \"A structured JSON object represented in canonical string form.\"","required":true},"litmus":{"description":"","type":"\"Not a verified fact; verification is application-level. Not what it represents.\" | \"Use when reversing subject and object changes the meaning and produces a different Statement identity.\" | \"Use when `A -> p -> B` means the same thing as `B -> p -> A`, such as identity or mutual-peer relations.\" | \"Not an account or organization.\" | \"Not a single person or software agent. Legal incorporation is not required, but a legal company is always an Organization. A branded initiative, product line, or program is usually a Concept unless it has its own durable collective agency. Not a query-defined group.\" | \"Not a person or organization. Not static passive code (use CreativeWork for repos, images, logic).\" | \"Not a subset or account. Not an inhabitable spatial context (use Place).\" | \"Authenticated by a host authority's database. This represents the account principal itself, not the agent or agents that operate it.\" | \"Authenticated via mathematical signature keys, independent of a host authority.\" | \"Not a raw value or formal concept. Not a running agent instance (use SoftwareAgent). Not a resource where location is primary identifier (use NetworkResource).\" | \"Not a plain raw value. Use this for labels, brands, programs, or initiatives that do not have their own durable collective agency.\" | \"Not a happening or a physical artifact. Can an Actor have presence there? Is its identity a spatial address rather than a document location or account?\" | \"Not a specific exertion of agency (Action).\" | \"Not a broad temporal happening (Event); must be driven by an Agent.\" | \"Not a location (where). Not a digital artifact (use CreativeWork).\" | \"Not a Concept.\" | \"Not a decimal or string.\" | \"Not an integer.\" | \"Not a number or string.\" | \"Not a datetime or time.\" | \"Not a full date or year alone.\" | \"Not a year-month or full date.\" | \"Not a date or datetime.\" | \"Not a date or time alone.\" | \"Not a datetime.\" | \"Not plain text.\" | \"Not a string.\"","required":true}}}],"packageName":"@fide-work/vocabulary"}} />
